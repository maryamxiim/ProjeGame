// import reactDom from "react-dom";
import react from "react";
// import "./style.css"

const Comp = (props)=> {
    return (<div className="Product" >
       <p> helllo wporldssss to {props.name} to number of {props.number}</p>
       {/* <p> helllo wporldssss to {props.name} to number of {props.number}</p> */}
    </div>)
}

export default Comp;